const response = require('../../../network/response');
const tryCatchFn = require('../../../utils/tryCatchFn');

module.exports = {
  getPublishers: tryCatchFn(async (req, res) => {

  }),
  postPublisher: tryCatchFn(async (req, res) => {

  }),
};
